{
  "name": "calculator",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass dist/public/main.scss dist/css/main.css --output-style compressed",
    "sass:watch": "npm run sass && node-sass dist/public/main.scss -wo dist"
  },
  "author": "Tycho Brouwer",
  "license": "ISC",
  "devDependencies": {
    "electron": "^14.0.0",
    "node-sass": "^6.0.1"
  }
}
